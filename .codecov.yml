codecov:
  require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        target: auto
        # informational: true forces the status to always post, even
        # without a baseline on main. Once a baseline is established,
        # this can be removed and the status will start gating PRs.
        informational: true
    patch:
      default:
        target: auto
    changes: false

flags:
  backend:
    paths:
      - doclatticeserver/
      - config/
    carryforward: true
  frontend-unit:
    paths:
      - frontend/src/
    carryforward: true
  frontend-component:
    paths:
      - frontend/src/
    carryforward: true
  frontend-e2e:
    paths:
      - frontend/src/
    carryforward: true
  backend-e2e:
    paths:
      - doclatticeserver/
      - config/
    carryforward: true

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: no

ignore:
  - "doclatticeserver/tasks/data_extract_tasks.py"
  - "doclatticeserver/tests/**"
  - "**/migrations/**"
  - "doclatticeserver/**/migrations/*.py"
  - "doclatticeserver/*/migrations/*.py"
  - "config/settings/test*.py"
  - "frontend/src/**/*.test.{ts,tsx}"
  - "frontend/src/setupTests.ts"
  - "frontend/tests/**"
  - "frontend/*.config.ts"
